<label class="list-group-item d-flex justify-content-between align-items-center"><%= "#{classroom.name}" %>
  <input <%= radio_button_tag "change[classroom_id]", classroom.id,
      class: "form-check-input me-2", data: { action: "change->transfers-classroom-selection#updateHiddenField change->unsaved-changes#checkChanges", classroom_id: classroom.id }, checked: classroom_id.to_i == classroom.id %></input>
</label>