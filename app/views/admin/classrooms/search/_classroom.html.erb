<label class="list-group-item d-flex justify-content-between align-items-center">
  <div>
    <%= radio_button_tag "schedule[classroom_id]", classroom.id,
        class: "form-check-input me-2", data: { action: "change->classroom-selection#selectclassroom", classroom_id: classroom.id } %>
    <%= "#{classroom.name}" %>
  </div>
</label>